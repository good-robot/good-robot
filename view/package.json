{
  "name": "rover-server",
  "version": "1.0.0",
  "description": "streaming server based on janus for remote controlling a bot",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "forever start server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "forever": "^1.0.0",
    "ws": "^7.2.0"
  }
}
